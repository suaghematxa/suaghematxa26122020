

<div class="ghemassages-all">
    <h2>Tổng số sản phẩm <%=response.count%></h2>
    <a href="/camera/camera-create">Thêm sản phẩm <i class="fa fa-plus"></i></a>
  <table class="table table-bordered table-admin">
    <thead>
      <tr>
        <th>Tên sản phẩm</th>
        <th>Tên Seo</th>
        <th>NSX</th>
        <th>Giá</th>
        <th>Tiêu đề</th>
        <th>Hình ảnh Default</th>
        <th>Hình ảnh thumb</th>
        <th>Sửa</th>
        <th>Xóa</th>
      </tr>
    </thead>
    <tbody>
      <% for(var i=0;i<response.count;i++){ %>
        <tr>
        <td> <%=response.cameras[i].name %> </td>
        <td> <%=response.cameras[i].nameseo %> </td>
        <td> <%=response.cameras[i].nsx %> </td>
        <td> <%=response.cameras[i].price %> </td>
        <td><%-response.cameras[i].title %>  </td>
        <td class="image-response">
          <%for(var n=0;n<response.cameras[i].imagedefault.length;n++){%>
          <img src="/assest/image/<%=response.cameras[i].imagedefault[n].originalname%>"width="50" />
          <%}%>
        </td>
        <td class="image-response">
          <%for(var k=0;k<response.cameras[i].image.length;k++){%>
          <img src="/assest/image/<%=response.cameras[i].image[k].originalname%>" width="50"/>
          <%}%>
        </td>
        <td><a href="/camera/edit/<%=response.cameras[i]._id %>">Sửa</a></td>
        <td><a class="delete-camera" data-id="<%=response.cameras[i]._id %>">Xóa</a></td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
